.space-y-24
  = MarkdownHandler.render(Blog.last&.content)
  -# = render 'hero'
  -# = render 'about_us'
  -# = render 'product_customization'
  -# = render 'product_costumization_feedback'
  -# = render 'want_product_customization'
  -#
